//Create a function that takes 3 arguments and 
//returns the sum of the these arguments.

function sum(x, y, z) {
    let plus = x + y + z;
    console.log(plus);
}

//check and example
sum(3, 4, 5);